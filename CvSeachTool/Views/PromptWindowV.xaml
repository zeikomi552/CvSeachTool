<mah:MetroWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvSeachTool.Views"
        xmlns:ViewModels="clr-namespace:CvSeachTool.ViewModels" x:Class="CvSeachTool.Views.PromptWindowV"
        mc:Ignorable="d"
        Title="PromptWindowV" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <mah:MetroWindow.DataContext>
        <ViewModels:PromptWindowVM/>
    </mah:MetroWindow.DataContext>
    <Grid>
        <DataGrid IsReadOnly="True" AutoGenerateColumns="False" Margin="5" ItemsSource="{Binding PromptItems.PromptItems.Items}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Prompt" Binding="{Binding Prompt}"/>
                <DataGridTextColumn Header="Count" Binding="{Binding Count}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</mah:MetroWindow>
